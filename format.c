#include <stdio.h>
/**
 * _printf - produces output
 * @format: specifier
 * Return: output
 */
int _printf(const char *format, ...)
{
char format;
format = "alphabet";

printf("%c" comes first in the "%s", "a", format);
return (0);
}
